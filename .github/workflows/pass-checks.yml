name: Pass Checks

on:
  workflow_dispatch:
    inputs:
      branch:
        description: Branch name to test
        required: true
        type: string
        default: main

jobs:
  pass-checks:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.branch }}

      - name: Start infrastructure
        run: docker compose up -d --wait

      - name: Wait for PowerSync
        run: |
          for i in $(seq 1 60); do
            if curl -sf http://localhost:8080/probes/liveness >/dev/null 2>&1; then
              echo "PowerSync is ready"
              exit 0
            fi
            sleep 2
          done
          echo "PowerSync did not become ready in time"
          docker compose logs
          exit 1

      - name: Seed Postgres
        run: |
          docker compose exec -T postgres psql -U postgres -d postgres -c \
            "INSERT INTO todos (id, title, is_completed, created_at) VALUES ('11111111-1111-4111-8111-111111111111', 'synced from postgres', 0, now()) ON CONFLICT (id) DO NOTHING;"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install backend dependencies
        run: pip install -r backend/requirements.txt

      - name: Start backend
        working-directory: backend
        run: uvicorn main:app --host 127.0.0.1 --port 8000 &

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Start frontend
        working-directory: frontend
        run: npm run dev -- --host 0.0.0.0 &

      - name: Wait for app services
        run: |
          npm install --no-save wait-on
          npx wait-on -t 120000 http://localhost:5173 http://127.0.0.1:8000/todos

      - name: Install Playwright
        working-directory: e2e
        run: |
          npm ci
          npx playwright install chromium --with-deps

      - name: Run pass checks
        working-directory: e2e
        run: npm test

      - name: Capture Docker logs on failure
        if: failure()
        run: docker compose logs > docker-logs.txt 2>&1

      - name: Upload debug artifacts
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pass-checks-debug-${{ github.run_id }}
          path: docker-logs.txt
